Your audit allowance is used.

<%= @shop.shop_name.presence || @shop.shopify_domain %> has used <%= @shop.audits_used %> of <%= @shop.audit_limit %> audits currently included on the <%= @shop.entitlement_plan.name %> plan.

<% if @shop.billing_trial? %>Paid plan allowances become available after the introductory trial.<% end %>

<% if @shop.billing_trial? %>Paid allowance begins after <%= @shop.billing_trial_ends_at.to_fs(:long) %>.<% else %>Allowance refreshes around <%= @shop.usage_period_ends_at.to_fs(:long) %>.<% end %> Existing results remain available.

Compare plans: <%= @plans_url %>

Plan update

<% if @shop.billing_active? %><%= @shop.billing_plan.name %> is now active for <%= @shop.shop_name.presence || @shop.shopify_domain %>.
Allowance: <% if @shop.billing_trial? %>1 audit total during the introductory trial<% else %><%= @shop.audit_limit %> audits every 30 days<% end %>, with the top 10 findings and AI-assisted guidance.
<% if @shop.billing_trial? %>Trial ends: <%= @shop.billing_trial_ends_at.to_fs(:long) %>
<% end %><% else %>Your previous paid plan is no longer active. Your store is now using Free: one audit every 30 days and the top five evidence-backed fixes.
<% end %>
Review plans: <%= @plans_url %>
